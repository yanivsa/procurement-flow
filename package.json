{
  "name": "procurement-flow-worker",
  "version": "0.2.0",
  "description": "Cloudflare Worker for the Purchase Committee Request Form with D1 + R2 bindings",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240524.0",
    "typescript": "^5.5.4",
    "wrangler": "^4.53.0"
  }
}
